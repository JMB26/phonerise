{% extends 'base.html.twig' %}

{% block title %}Detailcommande{% endblock %}

{% block body %}
    <h1>Detailcommande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detailcommande.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ detailcommande.titre }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ detailcommande.quantity }}</td>
            </tr>
            <tr>
                <th>Prix_unit</th>
                <td>{{ detailcommande.prixUnit }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ detailcommande.total }}</td>
            </tr>
            <tr>
                <th>Ref</th>
                <td>{{ detailcommande.ref }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_detailcommande_index') }}">back to list</a>

    <a href="{{ path('app_detailcommande_edit', {'id': detailcommande.id}) }}">edit</a>

    {{ include('detailcommande/_delete_form.html.twig') }}
{% endblock %}
